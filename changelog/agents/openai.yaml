interface:
  display_name: "Changelog"
  short_description: "Update dated changelog from Git history"
  default_prompt: "Use $changelog to refresh this project's CHANGELOG.md before merging."

policy:
  allow_implicit_invocation: false
